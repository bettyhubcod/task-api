server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task-core
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
